import sys
from pathlib import Path
import logging
logging.basicConfig(level=logging.DEBUG, format='%(levelname)s:%(name)s:%(message)s')
# Robustly add the project root (parent of this file's directory) to sys.path

logger = logging.getLogger(__name__)
from sec2md.core import convert_to_markdown

with open("/Users/nadavsmacbookair/Documents/one_table.html",'r') as f:
    html_file = f.read()

md = convert_to_markdown(
    html_file
)
